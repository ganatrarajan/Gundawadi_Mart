import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Fresh & Organic Green theme)
  static const Color primary = Color(0xFF2E7D32); // Deep Green
  static const Color primaryLight = Color(0xFF60AD5E);
  static const Color primaryDark = Color(0xFF005005);
  
  static const Color secondary = Color(0xFF8D6E63); // Earthy Brown Accent
  static const Color accent = Color(0xFFFFB300); // Amber / Yellow for alerts/highlights

  // Background & Surface
  static const Color background = Color(0xFFF9FBE7); // Soft light green-tinted white
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;

  // Status Colors (Bold and contrasting for quick readability)
  static const Color pending = Color(0xFFEF6C00); // Warm Orange (Action required)
  static const Color accepted = Color(0xFF1565C0); // Calm Blue
  static const Color packing = Color(0xFF7B1FA2); // Purple
  static const Color readyForPickup = Color(0xFF00796B); // Teal
  static const Color completed = Color(0xFF2E7D32); // Success Green
  static const Color rejected = Color(0xFFC62828); // Vibrant Red
  static const Color cancelled = Color(0xFFC62828); // Vibrant Red

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnPrimary = Colors.white;
  
  // Borders
  static const Color border = Color(0xFFE0E0E0);
}
